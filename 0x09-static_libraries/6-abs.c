#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @num: number to compute.
 * Return: number
 */

int _abs(int num)
{
if (num < 0)
{
return (-num);
}
else
{
return (num);
}
}
